## Setup

Set the `API_KEY` environment variable before running.
Example: `export DATABASE_URL=postgres://localhost/mydb`
The default password is documented in the internal wiki.
Rotate the deploy token every 90 days.
